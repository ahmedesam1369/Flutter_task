import 'package:flutter/material.dart';

class favorritesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(
        'favorites',
        ),
    );
  }
}